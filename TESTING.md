# 수동 브라우저 확인

현재 작업 환경에는 설치된 Chromium, Firefox, Playwright 또는 Puppeteer가 없어 네트워크 설치 없이 자동 브라우저 검사를 실행할 수 없다. 배포 전 다음을 실제 브라우저에서 확인한다.

- 320px 세로 화면에서 인트로, 판, 좌우 버튼이 잘리지 않는다.
- DPR 2 이상 화면에서 Canvas가 선명하고 크기 변경 후 게임 좌표가 유지된다.
- 화면 좌우 터치와 키보드 방향키가 각각 60도 회전한다.
- 탭 숨김과 창 포커스 상실 시 판이 가려지고, 계속하기 뒤 여러 칸을 건너뛰지 않는다.
- 카운트다운 중 이탈하면 복귀 후 3초부터 다시 센다.
- 종료 통계가 실제 기록과 같고 다시 하기가 모든 판 상태를 초기화한다.
- 음소거는 소리만 끄며 진동 미지원·저장 차단 환경에서도 게임이 계속된다.
