# 헥스 스네이크 PRD

> 이 문서는 헥스 스네이크에서 무엇을 만들지 계속 갱신하는 개발 지시서다.
> 최종 수정일: 2026-09-14

## 1. 한 줄 요약

육각형 칸으로 이루어진 육각 경기장에서 좌우 60도 회전으로 뱀을 조종하고, 먹이와 아이템·칸 사이 벽을 활용해 최고 점수에 도전하는 웹게임이다.

## 2. 왜 만드는가

기존 사각 격자 스네이크의 상하좌우 이동을 여섯 방향 이동으로 바꿔 새로운 경로 선택의 재미를 만든다. 속도·길이·벽·갑옷 아이템으로 매 판 다른 전략을 제공하되 기본 스네이크 규칙은 쉽게 알아볼 수 있게 유지한다.

## 3. 누가 쓰는가

- 설치나 로그인 없이 휴대폰에서 짧게 기록에 도전하려는 한 명의 사용자
- 화면 좌우 터치를 주로 사용하고 PC에서는 좌우 방향키를 사용한다.

## 4. 핵심 기능 목록과 우선순위

### 반드시 있어야 함(MVP)

- **F1. 육각 경기장과 이동**: 육각형 칸의 경기장과 여섯 방향 연속 이동을 제공한다.
- **F2. 먹이·성장·충돌**: 일반 먹이, 성장, 점수, 외곽·자기 몸 충돌과 종료를 처리한다.
- **F3. 특수 아이템**: 속도 증가·감속, 길이 감소, 벽 생성, 갑옷을 제공한다.
- **F4. 칸 사이 벽과 갑옷**: 중앙 부근 경계에 벽을 만들고 헬멧 상태로 한 번 파괴한다.
- **F5. 시작·결과·기록**: 카운트다운, 결과, 다시 하기와 최고 점수 저장을 제공한다.

### 있으면 좋음

- **F6. 피드백과 안전한 일시정지**: 효과음·진동·음소거와 화면 이탈 시 일시정지를 제공한다.

### 나중에

- 여러 경기장과 스테이지
- 온라인 순위, 로그인, 여러 기기 기록 동기화
- 추가 아이템과 스킨

## 5. 필수 기능별 상세

### F1. 육각 경기장과 이동

Canvas에 육각 칸으로 이루어진 육각형 경기장을 그린다. 뱀은 멈추지 않고 현재 방향으로 한 칸씩 이동한다. 화면 왼쪽 터치 또는 왼쪽 방향키는 60도 왼쪽, 화면 오른쪽 터치 또는 오른쪽 방향키는 60도 오른쪽으로 다음 이동 방향을 바꾼다.

완료 기준:

- [ ] 경기장 외곽과 내부 칸이 육각형 구조로 보인다.
- [ ] 뱀은 정확히 여섯 이웃 방향으로만 이동한다.
- [ ] 한 이동 사이클에 회전 입력은 한 번만 적용되어 자기 목 방향으로 즉시 역주행하지 않는다.
- [ ] 화면 좌우 터치와 키보드 좌우 방향키가 같은 결과를 낸다.
- [ ] 보드 밖, 버튼, 대화상자 입력은 방향 변경으로 처리하지 않는다.

예외 상황:

- 빠른 연속 입력은 다음 이동들을 위한 제한된 입력 대기열로 처리하며 무한히 쌓지 않는다.
- 화면 크기가 바뀌어도 게임 상태와 육각 좌표는 유지한다.

### F2. 먹이·성장·충돌

비어 있고 접근 가능한 칸에 먹이 또는 아이템 하나를 생성한다. 일반 먹이는 50% 확률이며 먹으면 100점과 함께 몸이 한 칸 길어진다. 일반 먹이 5개마다 기본 이동 속도가 한 단계 빨라지고 사람이 조작 가능한 최대 속도에서 고정된다. 경기장 밖으로 나가거나 머리가 자기 몸에 닿으면 즉시 종료한다.

완료 기준:

- [ ] 일반 먹이는 뱀과 겹치지 않는 유효한 칸에 하나만 나타난다.
- [ ] 일반 먹이를 먹으면 100점과 몸길이 한 칸이 정확히 추가된다.
- [ ] 일반 먹이 5개마다 기본 이동 간격이 감소하고 하한 아래로 내려가지 않는다.
- [ ] 외곽 또는 자기 몸 충돌은 한 번만 게임을 종료한다.
- [ ] 이동 가능한 빈칸이 없으면 승리 안내와 함께 안전하게 결과를 연다.

예외 상황:

- 먹이를 놓치는 개념은 없으며 먹을 때까지 해당 칸에 유지한다.
- 난수가 반복되어도 제한 횟수 뒤 유효한 빈칸을 결정적으로 선택한다.

### F3. 특수 아이템

새 대상 생성 시 속도 증가, 감속, 길이 감소, 벽, 갑옷 아이템은 각각 10% 확률이다. 특수 아이템은 각각 50점을 주고 몸은 늘리지 않는다. 외형은 기능을 즉시 이해할 수 있는 아케이드 아이콘으로 정한다.

- 속도 증가: 다음 일반 먹이 3개까지 이동 속도를 한 단계 빠르게 한다.
- 감속: 다음 일반 먹이 3개까지 이동 속도를 한 단계 느리게 한다.
- 반대 속도 효과를 먹으면 기존 효과를 교체한다.
- 길이 감소: 꼬리 3칸을 제거하되 최소 길이 3칸을 유지한다.
- 벽: 중앙 부근의 안전한 칸 경계에 벽 하나를 만든다.
- 갑옷: 벽 파괴 1회를 저장하고 머리에 헬멧을 표시한다.

완료 기준:

- [ ] 일반 50%와 다섯 아이템 각 10%의 경계를 주입 난수 테스트로 재현한다.
- [ ] 특수 아이템마다 50점만 추가되고 몸길이는 늘지 않는다.
- [ ] 속도 효과와 남은 일반 먹이 횟수가 화면에 표시되고 3개 뒤 해제된다.
- [ ] 반대 속도 효과는 중첩하지 않고 즉시 교체한다.
- [ ] 길이 감소 후에도 몸은 최소 3칸이다.
- [ ] 아이템 이미지, 이름, 색상 외 모양을 함께 사용해 구분한다.

예외 상황:

- 저장된 효과나 갑옷은 새 게임에 이어지지 않는다.
- 효과 적용 뒤 실제 이동 간격이 안전한 최소·최대 범위를 벗어나지 않는다.

### F4. 칸 사이 벽과 갑옷

벽은 칸을 차지하지 않고 인접한 두 육각 칸 사이의 공유 경계에 표시한다. 중앙 부근 후보 중 뱀 머리의 다음 경로, 현재 대상과 겹치지 않고 경기장을 완전히 막지 않는 곳에 생성한다. 한 판에 최대 5개를 유지한다. 갑옷 보유 중 생성된 벽에 충돌하면 해당 벽만 부수고 이동을 계속하며 갑옷과 헬멧을 소모한다. 외곽과 자기 몸 충돌에는 갑옷을 사용하지 않는다.

완료 기준:

- [ ] 벽은 유효한 두 칸 사이 경계에만 생성되고 양쪽 이동을 모두 막는다.
- [ ] 벽은 최대 5개이며 후보가 없으면 아이템 점수만 지급하고 안내한다.
- [ ] 갑옷 없이 벽에 닿으면 종료된다.
- [ ] 갑옷이 있으면 부딪힌 벽 하나만 제거하고 한 번 계속 이동한다.
- [ ] 갑옷 보유 중 머리에 헬멧이 보이고 소모 후 사라진다.
- [ ] 벽 생성으로 뱀을 즉시 가두거나 다음 이동에서 강제 충돌시키지 않는다.

예외 상황:

- 갑옷 보유 중 갑옷을 다시 먹으면 1회 상태를 유지하고 50점만 추가한다.
- 벽 데이터는 방향이 반대인 동일 경계를 중복 저장하지 않는다.

### F5. 시작·결과·기록

첫 화면에서 전체 규칙과 조작법을 보여준다. 시작 버튼을 누르면 3초 카운트다운 뒤 게임을 시작한다. 결과에는 이번 점수, 일반 먹이 수, 획득 아이템 수, 최고 점수를 표시하고 다시 하기를 제공한다. 로그인 없이 현재 브라우저에 최고 점수와 음소거 설정만 저장한다.

완료 기준:

- [ ] 시작 전 규칙과 좌우 조작, 아이템 효과를 확인할 수 있다.
- [ ] 3초 카운트다운이 끝나기 전에는 뱀이 움직이지 않는다.
- [ ] 결과 기록이 실제 한 판 값과 일치하고 결과는 한 번만 열린다.
- [ ] 다시 하기는 모든 게임 상태를 초기화한다.
- [ ] 최고 점수는 더 높은 기록일 때만 갱신되고 새로고침 후 유지된다.

예외 상황:

- 저장소가 없거나 깨졌거나 차단되면 기본값으로 계속하고 안내를 한 번 표시한다.
- 빠르게 시작·다시 하기를 반복해도 이동 타이머가 중복되지 않는다.
- 긴 사용자 입력과 삭제 데이터는 없으므로 잘림·삭제 복원 흐름은 만들지 않는다.

### F6. 피드백과 안전한 일시정지

먹이·각 아이템·충돌·벽 파괴에 구분되는 짧은 시각·소리·진동 효과를 제공한다. 음소거는 소리만 끈다. 탭 숨김 또는 창 비활성화 시 즉시 게임을 멈추고 판을 가리며 사용자가 계속하기를 눌러야 재개한다.

완료 기준:

- [ ] 첫 사용자 조작 전에는 소리를 자동 재생하지 않는다.
- [ ] 음소거 상태에서는 효과음이 없고 진동 미지원 환경도 정상 진행된다.
- [ ] 일시정지 중 이동 시간과 속도 효과 횟수가 변하지 않는다.
- [ ] 재개 전 게임판을 볼 수 없고 입력할 수 없다.
- [ ] 반복 일시정지·재개에도 이동 작업이 하나만 유지된다.

예외 상황:

- 카운트다운 중 화면 이탈 후에는 3초 준비를 처음부터 다시 시작한다.
- 결과 화면에서는 일시정지 화면을 열지 않는다.

## 6. 이번에 만들지 않는 것과 이유

- 로그인·서버 저장·온라인 순위: 핵심 한 기기 기록 도전에 필요하지 않다.
- 여러 경기장·추가 스킨·추가 아이템: 육각 이동과 현재 5개 아이템을 먼저 검증한다.
- 결제·광고·알림·공유: 핵심 게임 진행과 관련이 없다.
- 기록 삭제·복원: 최고 점수와 음소거만 저장하므로 첫 버전에서는 제외한다.

## 7. 다뤄야 하는 정보

- 육각 좌표, 이웃 관계, 경기장 유효 칸과 칸 사이 경계
- 뱀의 몸 좌표 순서, 현재 방향, 회전 입력 대기열, 성장 예약
- 현재 대상의 칸·종류, 점수, 일반 먹이 수, 아이템 수
- 기본·효과 적용 이동 간격, 효과 종류와 남은 먹이 횟수
- 벽 경계 최대 5개, 갑옷 보유 여부
- 게임 상태와 기기 최고 점수·음소거 설정

## 8. 반드시 지켜야 할 것

- 육각 좌표 계산과 화면 그림을 분리해 자동 테스트할 수 있어야 한다.
- 320px 모바일 세로 화면에서 판과 조작 안내가 잘리지 않아야 한다.
- 벽은 칸이 아니라 칸 사이 경계로 그려야 한다.
- 대상·뱀·벽이 불가능한 상태로 겹치지 않아야 한다.
- 시간 계산은 실제 경과 시간을 사용하고 느린 화면에서도 중복 이동하지 않아야 한다.
- 색상만으로 뱀·먹이·아이템·벽을 구분하지 않아야 한다.

## 9. 절대 하지 말아야 할 것

- 60도 단위가 아닌 임의 방향으로 이동하지 않는다.
- 갑옷으로 외곽이나 자기 몸 충돌을 막지 않는다.
- 벽을 칸 중앙 장애물로 표현하지 않는다.
- 길이 감소로 몸을 3칸보다 짧게 만들지 않는다.
- 속도 효과를 영구 적용하거나 서로 중첩하지 않는다.
- 저장·소리·진동 실패 때문에 게임을 종료하지 않는다.

## 10. 제약사항

- 플랫폼: 모바일 우선 웹, PC 브라우저 지원
- 기술: HTML, CSS, JavaScript Canvas
- 배포: GitHub Pages 정적 배포
- 운영: 로그인·별도 서버 없이 상시 공개 링크 제공
- 저장: 현재 브라우저 로컬 저장소
- 비용: 유료 서비스와 유료 소재 사용 안 함
- 사용 범위: 공개 링크를 아는 누구나 각자 한 기기에서 플레이

## 11. 성공 판단 기준

- 처음 보는 사용자가 육각형 여섯 방향과 좌우 회전을 이해한다.
- 아이템을 보고 효과를 예측하고 경로 선택에 활용한다.
- 벽과 갑옷으로 기존 스네이크와 다른 전략이 생긴다.
- 320px 화면에서 한 판을 오류 없이 끝내고 다시 시작할 수 있다.

## 12. 가정 및 확인 필요

- 일반 먹이 50%, 다섯 아이템 각각 10%로 확정했다.
- 속도 효과는 다음 일반 먹이 3개, 길이 감소는 3칸, 최소 몸길이는 3칸이다.
- 벽은 한 판 최대 5개이며 중앙 부근 안전 경계에 영구 유지된다.
- 갑옷은 벽 파괴 1회이며 머리 헬멧으로 표시한다.
- 아이템 최종 그림은 기능을 가장 빨리 이해할 수 있는 아케이드 이미지로 구현 단계에서 선택한다.
- 경기장 반지름과 정확한 이동 간격·속도 하한은 320px 플레이 가능성을 검증하며 정한다.

## 13. 아직 안 정한 것

- 경기장 육각 반지름과 총 칸 수
- 기본 이동 간격과 단계별 감소량
- 각 아이템의 최종 이미지
